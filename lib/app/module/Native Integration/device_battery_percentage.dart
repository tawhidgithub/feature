import 'package:flutter/material.dart';

class DeviceBatteryPercentage extends StatelessWidget {
  const DeviceBatteryPercentage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
